# Demeterics Managed LLM Key Configuration
# ========================================
#
# Get your key from: https://demeterics.com (Dashboard → Managed LLM Keys)
#
# Steps:
# 1. Sign up for a free account at https://demeterics.com
# 2. Open the Demeterics dashboard and click "Managed LLM Keys"
# 3. Create a new key (looks like dmt_xxxxx)
# 4. Rename this file to .env
# 5. Replace the placeholder below with your actual API key

DEMETERICS_API_KEY=dmt_your_api_key_here

# Notes:
# - Keep this key secret! Never commit .env to version control
# - The .gitignore file should already exclude .env
# - For bash/curl examples: export DEMETERICS_API_KEY="your_key_here"
# - For Node.js: Use dotenv package or export to environment
# - For Python: Use python-dotenv or export to environment
# - For Go: Use godotenv or export to environment
# - For Arduino: Edit the API key directly in the .ino file
# - One key unlocks Groq, OpenAI, Gemini, and Anthropic through the Demeterics proxy
